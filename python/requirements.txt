# The analysis engine needs only numpy. Everything else was removed:
# librosa/soundfile/rich/pymediainfo because all decoding goes through ffmpeg,
# and scipy because it was imported for a single function (fftconvolve) that
# numpy's own FFT reproduces exactly -- scipy alone was ~0.3s of the frozen
# sidecar's cold start and a large share of its size.
numpy>=1.24

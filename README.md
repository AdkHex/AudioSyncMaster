# AudioSync

Windows audio sync utility packaged with Tauri.

## Development

```sh
npm install
dev.bat
```

## Notes

- The UI lives in `src/`.
- Python workflows live in `Python Files/`.
- The Python bridge is in `python/bridge.py`.
